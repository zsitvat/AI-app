{
    "configurations": [
        {
            "type": "debugpy",
            "request": "launch",
            "name": "Launch AI-app",
            "program": "${workspaceFolder}/src/app.py",
            "console": "integratedTerminal",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}